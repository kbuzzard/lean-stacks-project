import data.equiv
#check equiv.left_inv
#check set.image -- I don't like ?M_1, no clue as to type
#check @set.image -- oh if I'd known they were alpha and beta
                  -- I would have known they were types. 
variables (α β : Type)
#check @set.image α β -- That's what I wanted to see the first time

#check equiv